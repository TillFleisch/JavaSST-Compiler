package dev.fleisch.JSSTCompiler;

/**
 * ENUM containing Types used by JavaSST.
 * An enum is fine, since JavaSST does not allow custom ObjectTypes and only has INTs and VOIDs.
 */
public enum Type {
    INT, VOID
}

